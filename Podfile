# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'search-filter-mini-proj' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for search-filter-mini-proj
  pod 'TTRangeSlider'
  pod 'SwiftHTTP'
  pod 'SwiftyJSON'
  pod 'WARangeSlider'

  target 'search-filter-mini-projTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'search-filter-mini-projUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
